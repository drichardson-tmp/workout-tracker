# Backend Environment Variables
# Dev default connects as the current OS user with no password (standard local Postgres install).
# Override for Docker or production.
DATABASE_URL=postgres://localhost/workout_tracker?sslmode=disable
PORT=8080

# Zitadel Authentication (optional — leave unset to disable auth in dev)
# Start Zitadel with: make auth-start
# Then open http://localhost:8081, finish setup, and create an API application.
# Set ZITADEL_PORT only for local/insecure instances; leave blank for cloud.
ZITADEL_DOMAIN=localhost
ZITADEL_PORT=8081
ZITADEL_CLIENT_ID=
ZITADEL_CLIENT_SECRET=
